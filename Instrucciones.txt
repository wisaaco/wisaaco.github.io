sudo apt install ruby-full
sudo gem install jekyll bundler



esto:
jekyll build

Haciendo el build se genera un folder "_site" que puede moverse a un servidor http:
tar -czvf webfile.tar.gz _site/



jekyll serve --host=0.0.0.0



o esto
bundle exec jekyll serve --host=0.0.0.0


El tema está disponible en:
https://github.com/ngzhio/jekyll-theme-hamilton



Algunos comandos prácticos:
scp -r webpersonal/ pi@192.168.1.22:/home/pi

bundle exec jekyll serve --host=0.0.0.0

tar -czvf webfile.tar.gz _site/

scp pi@192.168.1.22:/home/pi/webpersonal/webfile.tar.gz .

tar -xzvf webfile.tar.gz

scp -r * acsic@acsicpersonal.uib.es:~/www/isaaclera/


ssh acsic@acsicpersonal.uib.es